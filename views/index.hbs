<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Social Scanner</title>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.11.2/css/all.css">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.11.2/css/v4-shims.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="content">

    <p>I made this thing to check if usernames are avalable on some websites/services.</p>

    <form class="form" action="/" method="POST" role="form">
        <input type="hidden" id="g-recaptcha-response" name="g-recaptcha-response">
        <input type="text" name="username" placeholder="Username">
        <button type="submit" name="submit">CHECK</button>
    </form>
    {{#if message}}
    <center><span style='color: rgb(134, 0, 0);'>{{message}}</span></center>
    {{/if}}
    {{#if username}}
    <center><p>checked username <span style="color: rgb(255, 208, 0);">{{username}}</span></p></center>
    {{/if}}
    {{#if mediums}}
        <div class="row row-cols-1 row-cols-md-3">
        {{#each mediums}}
            <a href="{{this.link}}" target="_blank">
            <div class="col mb-4">
                {{#if availability}}
                    <div class="card h-100" style="background-color: #082600; border: 2px solid lime;">
                {{else}}
                    <div class="card h-100" style="background-color: #260000; border: 2px solid red;">
                {{/if}}
                <div class="card-body">
                    <h5 class="card-title"><i class="{{this.icon}}"></i><br> {{this.service_name}}</h5>
                    {{#if availability}}
                        <p class="card-text">AVAILABLE</p>
                    {{else}}
                        <p class="card-text">UNAVAILABLE</p>
                    {{/if}}
                </div>
                </div>
            </div>
        </a>
        {{/each}}
        </div>
    {{/if}}

    

    </div>
    </div>
</body>
</html>